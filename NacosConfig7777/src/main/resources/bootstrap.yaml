# nacos配置
server:
  port: 7777

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.159.1 #Nacos服务注册中心地址
      config:
        server-addr: 192.168.159.1 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置